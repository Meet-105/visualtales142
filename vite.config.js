import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  // IMPORTANT: Change '/portfolio/' to match your GitHub repository name exactly
  base: '/portfolio/', 
})
